public String nameFromEmail(String email) {
    //code here
    String first = substringBefore('.')+toLowerCase()+ capitalize();
    String last = substringBefore('@')+toLowerCase()+ capitalize();
    return first + ' '+last;
}