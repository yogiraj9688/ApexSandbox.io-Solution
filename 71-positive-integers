public List<Integer> positiveIntegers(List<Integer> numbers) {
    //code here
    List<Integer> positiveNum = new List<Integer>();
    for(Integer i =0;i<numbers.size();i++)
    {
        if(numbers[i]> 0)
        {
            positiveNum.add(numbers[i]);
        }
    }
    return positiveNum;
}